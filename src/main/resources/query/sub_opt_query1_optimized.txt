for $tuple in join (
for $a in doc("j_caesar.xml")//ACT, 
	$sp in $a//SPEAKER
return <tuple>{
	<a> {$a} </a>, 
	<sp> {$sp} </sp>
}</tuple>, 
for $s in doc("j_caesar.xml")//SPEAKER, 
	$sp in $a//SPEAKER, 
	$stxt in $s/text()
return <tuple>{
	<s> {$s} </s>, 
	<stxt> {$stxt} </stxt>
}</tuple>, 
	[sp], [s])
return <act> { $tuple/a/*/TITLE/text()} </act>